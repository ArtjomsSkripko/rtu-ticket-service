server:
  port: 8085
    
spring:

#====== Data source ========
  datasource:
    url: jdbc:postgresql://localhost:5432/regional
    username: user_name
    password: password
    driverClassName: org.postgresql.Driver
    platform: postgresql
    initialization-mode: never
    continueOnError: true
    sqlScriptEncoding: UTF-8
